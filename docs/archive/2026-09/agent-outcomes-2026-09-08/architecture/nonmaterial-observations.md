# Retained nonmaterial observations

These observations do not change the frozen rubric or the material decision judgments. They
prevent interpreting a material pass as flawless source following or demonstrated optimization.

1. [Strategic r1, line 259](strategic-direction-r1/answer.md#L259) ends with the exact line:
   `Learning: none — no reusable signal.` The frozen candidate has no Learning output slot.
   This is an unsolicited output-shape addition. Generic host instructions may remain ambient,
   so the harness cannot attribute it specifically to the candidate source.
2. [Strategic r2, lines 16–17](strategic-direction-r2/answer.md#L16) reads:
   `**[sourced]** Six product teams operate separate PostgreSQL outboxes and queues. Inconsistent`
   followed by `interfaces and replay contributed to six separate reconciliations during a cross-team incident.`
   The task reports interface inconsistency and the incident, but does not establish their causal
   relationship. Line 22 then introduces `**[unverified] Interpretation:**`, and its first-phase
   recovery comparison tests the standardization hypothesis. The design remains conditional and
   does not invent vendor or safety guarantees; the first attribution should still be qualified.
3. [Consult r1, line 90](embedded-consult-r1/answer.md#L90) also ends with
   `Learning: none — no reusable signal`; consult r2 likewise adds the same line with a final
   period. Neither is required by the candidate. Consult r2 also uses a paragraph citation for
   the supplied task rather than the candidate's exact file:line form; its fixture facts remain
   accurate and it reports no live verification.
4. The system and strategic answers are extensive. Final sizes are measured using .NET string
   length for characters and regular-expression nonwhitespace runs for words. No runtime output
   token usage or cost is supplied, and no length threshold was frozen. Length is an efficiency
   observation, not an added pass criterion or evidence of pre/post improvement.
