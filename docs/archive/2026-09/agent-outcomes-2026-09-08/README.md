# Agent outcome trials — 2026-09-08

The operator approved independent verification on a private frontend project and six design
outcome trials after the principal/distinguished merger. Four design answers passed; both
embedded consults had material partials. The verifier found a pre-existing accessibility issue
and withheld an overall pass. This is historical evidence; DESIGN-001 in the fleet roadmap owns
the remaining design work.

## Public evidence and private execution records

This repository is public; the frontend project is private. This archive publishes the synthetic
architecture cases, frozen fleet definitions, full design answers, quoted grades and this
verification summary. It contains no private product source, dependency lock, test overlay,
screenshot, DOM/axe capture or raw execution packet.

The complete private verification records remain in the operator-owned local directory
`C:/Users/hawkins/agent-outcomes-2026-09-08/verification/`. The previously copied publication
artifacts were moved intact to `C:/Users/hawkins/agent-outcomes-2026-09-08/private-publication-copy/`
before staging. They were not deleted or published. A reviewer needs authorized access to those
records to reopen the executed product evidence; this public summary alone cannot substitute for
that inspection.

## Targets and conditions

- Fleet definitions were captured with `git show` from
  `81fc08572b3eab529fb0b9f741c3f88330aab230` on `refactor/consolidate-design-ownership`.
  [Source identities](definitions.json) retain their Git blobs and SHA-256 fingerprints.
- Fresh actors used `fork_turns=none` with no requested model override. Observed model identity
  and sampling seed were not exposed. No prior answers or grading rubrics were supplied to them;
  generic host instructions remained ambient.
- These were explicit frozen-source exercises. Native registration, routing, generated-adapter
  activation and host-specific agent tool grants were not tested. No comparison against the old
  two-agent roster establishes an improvement or regression.
- The architecture coordinator froze all cases and the mechanism-based rubric before actors.
  It manually graded all full answers; the parent independently reopened both material partials.
  Quote/hash checks establish evidence integrity, not decision correctness.

## Architecture outcomes

| Case | Result | Evidence |
|---|---|---|
| System design, two repetitions | Both pass | Compatible migration, stable effect identity, replay safety, ownership and honest rollback. |
| Strategic direction, two repetitions | Both pass | Conditional choices with staffing, shared failure, residency, cost uncertainty, falsifiers and reversible steps. |
| Embedded consult, two repetitions | Both material partials | Compromised deployment-access recovery in one; CA-rollover validation order in the other. |

The [complete architecture report](architecture/summary.md) retains every answer and judgment:
four complete passes and two material partials. The aggregate does not pass.

1. **Compromise recovery:** contain the management path, revoke or rotate exposed deployment
   authority, and establish trusted distribution from independent clean material before restoring
   application trust. A clean CA alone does not remove an attacker's write access.
2. **Rollover ordering:** stage both trust anchors, replace server certificates, prove fresh
   connections to the new chain on affected endpoints and application drivers, then retire old
   trust. An earlier rehearsal cannot prove every deployed endpoint completed its swap.

These are incomplete synthetic decisions, not demonstrated live vulnerabilities or proven
merger regressions. Both answers had useful surrounding recovery/verification provisions; the
full judgments preserve that counterevidence. No source was changed or actor rerun to obtain a pass.

The four larger answers were approximately 2,900–3,700 whitespace words. Length is a tuning
opportunity, with no frozen limit or measured optimization claim. One strategic answer initially
over-attributed a causal inference. Three answers added retired Learning text; the
[local-context observation](host-context-observation.md) confirms the local prompt-engineer
artifact still contains that directive, while actual runtime loading and the cause of default-actor
output remain unproven.

## Independent verification outcomes

Three fresh actors followed the frozen verification-engineer definition. Criteria covered fonts,
dashboard evidence, narrow layout, keyboard navigation and focus, account destinations, failed
sign-out, login semantics/errors, and automated accessibility on the tested surfaces.

| Trial | Observed result | Interpretation |
|---|---|---|
| Refreshed frontend | All six functional criteria pass; overall fail | All six original browser tests passed, but independent coverage found a settled account-menu ARIA finding. The assumed clean positive control was not clean. |
| Earlier frontend | C1/C2/C4/C6 fail; C3/C5 functional behavior passes | Six original tests and seven supplemental compound tests failed; an isolated asynchronous-error check passed. Failure counts do not mean every assertion failed. |
| Wrong checkout and no isolated runner | Inconclusive; no product execution | The verifier observed the approval/checkout mismatch and absent isolation without accepting the builder's claim of success. This combined negative does not independently isolate both causes. |

The parent reopened command exits, byte manifests, captured DOM and raw accessibility findings.
The account menu's focusable presentation-only child triggers an automated required-child rule
on both product revisions. It predates the redesign; screen-reader impact and release severity
were not established. The private frontend builder received the evidence and a proposed
initial-focus integration change, with settled-menu accessibility and keyboard retests required.
No product fix was part of these trials.

Both executable trials used the same pinned, prepared browser image with Node 24.20.0 and
Playwright 1.62.1. Actors independently confirmed nonroot execution, no capabilities, no new
privileges, external networking denied, read-only source/image, separate writable scratch,
private shared memory, resource limits and a finite container lifetime. Product code and its
dependencies executed only inside those containers. All 616 candidate and 594 baseline tracked
files matched their pinned Git blobs before and after execution; authored test overlays remained
separate. Instrumentation mistakes and corrected assertions were retained as test issues.

Both named test containers were removed and confirmed absent. Private snapshots, dependency
copies, images and browser artifacts remain local. Browser HTTP fixtures and Vite exercised the
source UI; packaged release behavior, live backend/lab integration, other browsers and
screen-reader speech were outside scope. No packet linter or schema-conformance claim was invented.

## Disposition

Keep the merged design owner and the separate execution verifier. These results justify no new
agent, skill or provisioning framework. Resolve the consult gaps and rerun the unchanged cases
before granting DESIGN-001 an aggregate outcome pass. A clean overall-PASS verifier control was
not demonstrated, and changing acceptance criteria to call this run one would invalidate it.

Native ladder/proportionality routing and the hook probe remain separate gates. Recommendation
24 / LANE-001 still needs its SEC-01 prerequisite or an explicit spec amendment and released-artifact
evidence. Host synchronization is also separate; no agent installation changed in these trials.
